server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static files normally
    location / {
        try_files $uri /index.html;
    }

    # Disable .htaccess or hidden files access
    location ~ /\. {
        deny all;
    }
}